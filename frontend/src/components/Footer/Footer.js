import React from "react";
import styles from "./Footer.module.css";

const Footer = () => {
  return (
    <footer>
      <section className="container">Footer Teste</section>
    </footer>
  );
};

export default Footer;
